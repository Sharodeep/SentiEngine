1. Run the following command to install all required dependencies -
pip install -r requirements.txt
2.Use main.py to start the application, the first start up takes time and requires you to have an active internet connection to download a few nltk databases.